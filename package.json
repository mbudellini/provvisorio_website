{
  "name": "provvisorio",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend/admin\" \"npm run dev --prefix frontend/store\" \"npm run dev --prefix provvisorio_backend\"",
    "dev:admin": "npm run dev --prefix frontend/admin",
    "dev:store": "npm run dev --prefix frontend/store",
    "dev:backend": "npm run dev --prefix provvisorio_backend",
    "seed-admin": "node provvisorio_backend/scripts/seedAdmin.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}